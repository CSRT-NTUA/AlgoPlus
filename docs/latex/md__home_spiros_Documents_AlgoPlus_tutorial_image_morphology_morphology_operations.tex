\begin{DoxyVerb}--- dilate() function performs dilation to the passed image
--- erote() function performs erotion to the passed image
--- close() function performs closing to the passed image
--- open() function performs opening to the passed image
\end{DoxyVerb}
 \hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_morphology_morphology_operations_autotoc_md65}{}\doxysubsection{$<$strong$>$\+How to apply the operations to images$<$/strong$>$\+:}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_morphology_morphology_operations_autotoc_md65}

\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keywordtype}{int} main() \{}
\DoxyCodeLine{  \textcolor{comment}{// let's assume we have the data of the image in data.json file }}
\DoxyCodeLine{  std::ifstream ifs(\textcolor{stringliteral}{"{}data.json"{}});}
\DoxyCodeLine{  json jf = json::parse(ifs);}
\DoxyCodeLine{  \textcolor{comment}{// now 2d vector data has the data of the image}}
\DoxyCodeLine{  std::vector<std::vector<int32\_t>> data = jf[\textcolor{stringliteral}{"{}img"{}}];}
\DoxyCodeLine{  \mbox{\hyperlink{classImage}{Image}} img(data);}
\DoxyCodeLine{  \textcolor{comment}{// this applies the median filter to the image:img and saves the results to the res 2d array}}
\DoxyCodeLine{  std::vector<std::vector<int32\_t>> res = erote(img);}
\DoxyCodeLine{  std::vector<std::vector<int32\_t>> res = dilate(img);}
\DoxyCodeLine{  std::vector<std::vector<int32\_t>> res = open(img);}
\DoxyCodeLine{  std::vector<std::vector<int32\_t>> res = close(img);}
\DoxyCodeLine{  json j;}
\DoxyCodeLine{  j[\textcolor{stringliteral}{"{}data"{}}] = res;}
\DoxyCodeLine{  \textcolor{comment}{// we optionally write the results to a file}}
\DoxyCodeLine{  std::ofstream file(\textcolor{stringliteral}{"{}results.json"{}});}
\DoxyCodeLine{  file << j;}
\DoxyCodeLine{\}}

\end{DoxyCode}
\hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_morphology_morphology_operations_autotoc_md66}{}\doxysubsection{$<$strong$>$\+Visualize your results$<$/strong$>$\+:}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_morphology_morphology_operations_autotoc_md66}

\begin{DoxyCode}{0}
\DoxyCodeLine{import matplotlib.pyplot as plt}
\DoxyCodeLine{import json}
\DoxyCodeLine{import os}
\DoxyCodeLine{}
\DoxyCodeLine{\# Visualization can easily be achieved using matplotlib}
\DoxyCodeLine{\# We just open the file that we saved our results and plot the image}
\DoxyCodeLine{if \_\_name\_\_ == "{}\_\_main\_\_"{}:}
\DoxyCodeLine{    os.system("{}c++ -\/std=c++17 main.cc"{})}
\DoxyCodeLine{    os.system("{}./a.out"{})}
\DoxyCodeLine{    filename = open("{}results.json"{})}
\DoxyCodeLine{    file = json.load(filename)}
\DoxyCodeLine{    data = file['data']}
\DoxyCodeLine{    plt.imshow(data, cmap='gray')}
\DoxyCodeLine{    plt.show()}

\end{DoxyCode}
 