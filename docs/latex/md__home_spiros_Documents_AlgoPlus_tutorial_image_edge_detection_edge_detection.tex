\begin{DoxyVerb}--- We use sobel operator to perform edge detection. Sobel() function will return a 2d array with the resulted image
\end{DoxyVerb}
 \hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md50}{}\doxysubsection{$<$strong$>$\+Perform edge detection with Sobel() function$<$/strong$>$\+:}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md50}

\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keywordtype}{int} main()\{}
\DoxyCodeLine{  \textcolor{comment}{// data.json is the file that contains the 2d array of the image at "{}img"{} instance}}
\DoxyCodeLine{  std::ifstream ifs(\textcolor{stringliteral}{"{}data.json"{}});}
\DoxyCodeLine{  json jf = json::parse(ifs); }
\DoxyCodeLine{  \textcolor{comment}{// 2d vector "{}data"{} has now the data of the image }}
\DoxyCodeLine{  std::vector<std::vector<int32\_t> > data = jf[\textcolor{stringliteral}{"{}img"{}}];}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{comment}{// using the Sobel() function, passing the data as parameter, we perform edge detection. The results are saved at the 2d array "{}res"{}}}
\DoxyCodeLine{  std::vector<std::vector<int32\_t> > res = \mbox{\hyperlink{namespacesobel_ac1800919fa731d7cd354da961a98b77b}{Sobel}}(data);}
\DoxyCodeLine{  json j;}
\DoxyCodeLine{  j[\textcolor{stringliteral}{"{}data"{}}] = res;}
\DoxyCodeLine{  \textcolor{comment}{// we can optionally write the results in a file}}
\DoxyCodeLine{  std::ofstream file(\textcolor{stringliteral}{"{}results.json"{}});}
\DoxyCodeLine{  file << j;}
\DoxyCodeLine{\}}

\end{DoxyCode}
\hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md51}{}\doxysubsection{$<$strong$>$\+Visualize your results$<$/strong$>$\+:}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md51}

\begin{DoxyCode}{0}
\DoxyCodeLine{import matplotlib.pyplot as plt}
\DoxyCodeLine{import json}
\DoxyCodeLine{import os}
\DoxyCodeLine{}
\DoxyCodeLine{\# Visualization can easily be achieved using matplotlib}
\DoxyCodeLine{\# We just have to read from the results.json file and then plot the data with plt.imshow()}
\DoxyCodeLine{if \_\_name\_\_ == "{}\_\_main\_\_"{}:}
\DoxyCodeLine{    os.system("{}c++ -\/std=c++17 main.cc"{})}
\DoxyCodeLine{    os.system("{}./a.out"{})}
\DoxyCodeLine{    filename = open("{}results.json"{})}
\DoxyCodeLine{    file = json.load(filename)}
\DoxyCodeLine{    data = file['data']}
\DoxyCodeLine{    plt.imshow(data, cmap='gray')}
\DoxyCodeLine{    plt.show()}

\end{DoxyCode}
\hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md52}{}\doxysubsection{Mini tutorial for prewitt namespace}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md52}
--- We use prewitt operator to perform edge detection. Sobel() function will return a 2d array with the resulted image\hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md53}{}\doxysubsection{$<$strong$>$\+Perform edge detection with Prewitt() function$<$/strong$>$\+:}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md53}

\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keywordtype}{int} main()\{}
\DoxyCodeLine{  \textcolor{comment}{// data.json is the file that contains the 2d array of the image at "{}img"{} instance}}
\DoxyCodeLine{  std::ifstream ifs(\textcolor{stringliteral}{"{}data.json"{}});}
\DoxyCodeLine{  json jf = json::parse(ifs); }
\DoxyCodeLine{  \textcolor{comment}{// 2d vector "{}data"{} has now the data of the image }}
\DoxyCodeLine{  std::vector<std::vector<int32\_t> > data = jf[\textcolor{stringliteral}{"{}img"{}}];}
\DoxyCodeLine{}
\DoxyCodeLine{  \textcolor{comment}{// using the Sobel() function, passing the data as parameter, we perform edge detection. The results are saved at the 2d array "{}res"{}}}
\DoxyCodeLine{  std::vector<std::vector<int32\_t> > res = \mbox{\hyperlink{namespaceprewitt_a277066f6a4836b998d2cb6174301ad03}{Prewitt}}(data);}
\DoxyCodeLine{  json j;}
\DoxyCodeLine{  j[\textcolor{stringliteral}{"{}data"{}}] = res;}
\DoxyCodeLine{  \textcolor{comment}{// we can optionally write the results in a file}}
\DoxyCodeLine{  std::ofstream file(\textcolor{stringliteral}{"{}results.json"{}});}
\DoxyCodeLine{  file << j;}
\DoxyCodeLine{\}}

\end{DoxyCode}
\hypertarget{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md54}{}\doxysubsection{$<$strong$>$\+Visualize your results$<$/strong$>$\+:}\label{md__home_spiros_Documents_AlgoPlus_tutorial_image_edge_detection_edge_detection_autotoc_md54}

\begin{DoxyCode}{0}
\DoxyCodeLine{import matplotlib.pyplot as plt}
\DoxyCodeLine{import json}
\DoxyCodeLine{import os}
\DoxyCodeLine{}
\DoxyCodeLine{\# Visualization can easily be achieved using matplotlib}
\DoxyCodeLine{\# We just have to read from the results.json file and then plot the data with plt.imshow()}
\DoxyCodeLine{if \_\_name\_\_ == "{}\_\_main\_\_"{}:}
\DoxyCodeLine{    os.system("{}c++ -\/std=c++17 main.cc"{})}
\DoxyCodeLine{    os.system("{}./a.out"{})}
\DoxyCodeLine{    filename = open("{}results.json"{})}
\DoxyCodeLine{    file = json.load(filename)}
\DoxyCodeLine{    data = file['data']}
\DoxyCodeLine{    plt.imshow(data, cmap='gray')}
\DoxyCodeLine{    plt.show()}

\end{DoxyCode}
 